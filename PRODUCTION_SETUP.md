# Настройка Production (PostgreSQL + Vercel)

## 🎯 Быстрая настройка для Vercel с Neon PostgreSQL

### Шаг 1: Подготовка схемы Prisma для PostgreSQL

Схема для PostgreSQL уже создана в `prisma/schema.postgresql.prisma`.

### Шаг 2: Переключение на PostgreSQL схему

```bash
# Переключиться на PostgreSQL схему
npm run db:switch:postgres

# Сгенерировать Prisma Client
npm run db:generate
```

### Шаг 3: Настройка переменных окружения в Vercel

1. Зайдите в настройки проекта на Vercel
2. Перейдите в раздел **Environment Variables**
3. Добавьте следующие переменные:

```
DATABASE_URL=postgresql://neondb_owner:npg_FPGKV8QfJCB0@ep-sweet-term-ah6ieptn-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require
JWT_SECRET=Fff3f33f39901931r3Fff3f33f399019
```

**Важно:** 
- `DATABASE_URL` - используйте строку подключения из Neon
- `JWT_SECRET` - ваш секретный ключ (уже предоставлен выше)
- Убедитесь, что обе переменные добавлены для всех окружений (Production, Preview, Development)

### Шаг 4: Применение миграций

После первого деплоя выполните миграции:

**Вариант 1: Через Vercel CLI**

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Подключите проект
vercel link

# Получите переменные окружения
vercel env pull .env.local

# Примените миграции
npx prisma migrate deploy
```

**Вариант 2: Через package.json (автоматически при деплое)**

Добавьте в `package.json`:

```json
{
  "scripts": {
    "postinstall": "prisma generate",
    "vercel-build": "prisma generate && prisma migrate deploy && next build"
  }
}
```

### Шаг 5: Деплой

1. Запушьте изменения в GitHub
2. Vercel автоматически задеплоит проект
3. После деплоя проверьте логи в Vercel Dashboard

## 🔄 Переключение между SQLite (локально) и PostgreSQL (production)

### Для локальной разработки (SQLite):

```bash
# Переключиться на SQLite
npm run db:switch:sqlite

# Сгенерировать Prisma Client
npm run db:generate

# Применить схему
npm run db:push
```

### Для production (PostgreSQL):

```bash
# Переключиться на PostgreSQL
npm run db:switch:postgres

# Сгенерировать Prisma Client
npm run db:generate

# Создать и применить миграции
npx prisma migrate dev --name init
```

## 📝 Важные изменения в коде

Код обновлен для работы с обеими БД:

1. **Утилиты для работы с JSON** (`src/lib/db-utils.ts`):
   - `parseJsonField()` - парсит JSON из БД (работает и со String и с Json)
   - `serializeJsonField()` - сериализует данные для сохранения

2. **Обновленные API routes**:
   - Все места, где используется `JSON.parse()`, заменены на `parseJsonField()`
   - Это обеспечивает совместимость с SQLite (String) и PostgreSQL (Json)

## ✅ Проверка работы

После деплоя проверьте:

1. **Авторизация работает:**
   - Зарегистрируйте нового пользователя
   - Войдите в систему

2. **Создание компании:**
   - Создайте юридическое лицо
   - Добавьте API ключ Wildberries

3. **Генерация биллинга:**
   - Создайте биллинг
   - Сгенерируйте расчет

## 🐛 Решение проблем

### Ошибка: "Unknown argument" при работе с Prisma

**Решение:** Убедитесь, что Prisma Client сгенерирован с правильной схемой:
```bash
npm run db:switch:postgres
npm run db:generate
```

### Ошибка: "Database does not exist"

**Решение:** Проверьте строку подключения `DATABASE_URL` в настройках Vercel.

### Ошибка: "Migration failed"

**Решение:** Выполните миграции вручную:
```bash
npx prisma migrate deploy
```

### Ошибка: "Type error" при работе с JSON полями

**Решение:** Убедитесь, что используете `parseJsonField()` и `serializeJsonField()` из `src/lib/db-utils.ts`.

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи в Vercel Dashboard
2. Переменные окружения в Vercel
3. Статус базы данных в Neon
4. Миграции Prisma

## 🔐 Безопасность

- **Никогда не коммитьте** `.env` файлы в git
- Используйте сильный `JWT_SECRET` (минимум 32 символа)
- Регулярно обновляйте зависимости
- Используйте HTTPS для production


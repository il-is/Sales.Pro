# Sales.Pro

SaaS решение для автоматизации составления биллинга для склада ответственного хранения с интеграцией маркетплейсов (Wildberries и др.).

## 🚀 Технологии

- **Next.js 14** (App Router)
- **TypeScript**
- **Material-UI (MUI)** - современный UI
- **Prisma ORM** - работа с БД
- **SQLite** (локально) / **PostgreSQL** (production)
- **JWT** - авторизация
- **Zustand** - управление состоянием

## 📋 Функциональность

- ✅ Система авторизации (регистрация, вход)
- ✅ Управление юридическими лицами (CRUD)
- ✅ Настройка биллинга для каждого юридического лица
- ✅ Интеграция с Wildberries API
- ✅ Автоматический расчет биллинга на основе данных маркетплейса
- ✅ Просмотр детальной информации о биллингах
- 🔄 Экспорт в PDF и Excel (в разработке)

## 🛠 Установка для локальной разработки

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/il-is/Sales.Pro.git
cd Sales.Pro
```

### 2. Установите зависимости

```bash
npm install
```

### 3. Настройте базу данных

Создайте файл `.env` в корне проекта:

```env
DATABASE_URL="file:./dev.db"
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
```

### 4. Примените схему Prisma

```bash
npm run db:generate
npm run db:push
```

### 5. Запустите dev сервер

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📚 Доступные команды

- `npm run dev` - запуск dev сервера
- `npm run build` - сборка для production
- `npm run start` - запуск production сервера
- `npm run lint` - проверка кода
- `npm run db:generate` - генерация Prisma Client
- `npm run db:push` - применение схемы к БД (без миграций)
- `npm run db:migrate` - создание и применение миграций
- `npm run db:studio` - открытие Prisma Studio

## 🌐 Деплой на Vercel

### Важно: Миграция с SQLite на PostgreSQL

Для деплоя на Vercel необходимо использовать PostgreSQL, так как SQLite не поддерживается на серверах Vercel.

#### Шаги для деплоя:

1. **Создайте проект на Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Импортируйте репозиторий GitHub
   - Vercel автоматически определит Next.js проект

2. **Настройте PostgreSQL:**
   - В настройках проекта Vercel добавьте **Vercel Postgres** (или используйте внешний PostgreSQL)
   - Скопируйте строку подключения `DATABASE_URL`

3. **Обновите Prisma schema для production:**
   
   В `prisma/schema.prisma` измените:
   ```prisma
   datasource db {
     provider = "postgresql"  // вместо "sqlite"
     url      = env("DATABASE_URL")
   }
   ```
   
   И обновите типы полей:
   - `String` для JSON полей → `Json` (если нужно)
   - `String` для enum → `enum` (если нужно)

4. **Настройте переменные окружения в Vercel:**
   - `DATABASE_URL` - строка подключения к PostgreSQL
   - `JWT_SECRET` - секретный ключ для JWT (сгенерируйте случайную строку)

5. **Деплой:**
   - Vercel автоматически соберет и задеплоит проект
   - После деплоя выполните миграции:
     ```bash
     npx prisma migrate deploy
     ```
     (или через Vercel CLI)

### Альтернатива: Использование разных БД для dev и production

Можно настроить проект для использования SQLite локально и PostgreSQL в production:

1. Создайте два файла схемы: `schema.prisma` (SQLite) и `schema.postgresql.prisma` (PostgreSQL)
2. Используйте переменную окружения для выбора схемы
3. Или используйте Prisma Migrate с разными окружениями

## 📖 Документация

- `PROJECT_STRUCTURE.md` - детальная структура проекта
- `SETUP.md` - инструкции по настройке
- `BILLING_LOGIC.md` - логика расчета биллинга

## 🔐 Безопасность

- Все пароли хешируются с помощью bcrypt
- JWT токены используются для авторизации
- API ключи хранятся в базе данных (в production рекомендуется шифрование)
- `.env` файлы не коммитятся в репозиторий

## 📝 Лицензия

Private project

## 👤 Автор

Sales.Pro - автоматизация биллинга для склада ответственного хранения

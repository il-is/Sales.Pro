# 🚀 Быстрая настройка для Vercel

## Шаг 1: Подключение к Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите "New Project"
3. Импортируйте репозиторий `il-is/Sales.Pro` из GitHub

## Шаг 2: Настройка переменных окружения

В настройках проекта Vercel → **Environment Variables** добавьте:

```
DATABASE_URL=postgresql://neondb_owner:npg_FPGKV8QfJCB0@ep-sweet-term-ah6ieptn-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-this
```

**Важно:** Замените `JWT_SECRET` на случайную строку (минимум 32 символа). Можно сгенерировать через:
```bash
openssl rand -base64 32
```

## Шаг 3: Обновление схемы Prisma для PostgreSQL

Перед первым деплоем нужно переключить схему на PostgreSQL:

```bash
# Переключиться на PostgreSQL схему
npm run db:switch:postgres

# Закоммитить изменения
git add prisma/schema.prisma
git commit -m "Switch to PostgreSQL schema for production"
git push
```

## Шаг 4: Настройка автоматических миграций

Добавьте в `package.json` (уже добавлено):

```json
{
  "scripts": {
    "vercel-build": "prisma generate && prisma migrate deploy && next build"
  }
}
```

Или используйте `postinstall`:

```json
{
  "scripts": {
    "postinstall": "prisma generate"
  }
}
```

## Шаг 5: Первый деплой

1. Vercel автоматически задеплоит проект после пуша в GitHub
2. После деплоя выполните миграции через Vercel CLI:

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Подключите проект
vercel link

# Получите переменные окружения
vercel env pull .env.local

# Примените миграции
npx prisma migrate deploy
```

Или выполните миграции вручную через Neon Dashboard.

## Шаг 6: Проверка работы

После деплоя проверьте:

1. Откройте URL вашего проекта (например, `https://sales-pro.vercel.app`)
2. Зарегистрируйте нового пользователя
3. Создайте компанию
4. Проверьте работу системы

## 🔄 Возврат к локальной разработке (SQLite)

После работы с production вернитесь к SQLite для локальной разработки:

```bash
# Переключиться на SQLite
npm run db:switch:sqlite

# Сгенерировать Prisma Client
npm run db:generate

# Применить схему
npm run db:push
```

## 📝 Важные замечания

- **Локально** используйте SQLite (быстро, не требует настройки)
- **В production** используйте PostgreSQL (Neon)
- Код автоматически работает с обеими БД благодаря утилитам в `src/lib/db-utils.ts`
- Не коммитьте `.env` файлы в git

## 🐛 Решение проблем

### Ошибка при деплое: "Prisma schema not found"

**Решение:** Убедитесь, что `prisma/schema.prisma` использует PostgreSQL схему перед деплоем.

### Ошибка: "Migration failed"

**Решение:** Выполните миграции вручную:
```bash
npx prisma migrate deploy
```

### Ошибка: "Database connection failed"

**Решение:** Проверьте `DATABASE_URL` в настройках Vercel. Убедитесь, что используется правильная строка подключения из Neon.

## 📞 Дополнительная информация

Подробная инструкция в файле `PRODUCTION_SETUP.md`.

